参考“烧录固件引脚清单.md”，完成STM32嵌入式程序设计
- cubemx产生的ioc文件要同步更新
- 两块AD7606同步采样，量程为正负5V，采样频率2000hz
- 开启当前连接的这块DAC
- 通过串口USART1实时发送16个通道的ADC数值，2000hz
- 通过串口指令发送DAC要输出的值
- 保证各功能正常运行，不要阻塞
- 参考文件中使用了但在这没提的功能不用加
- 完成后git本地进行版本管理
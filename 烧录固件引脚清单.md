# 实际烧录固件 —— STM32F407 引脚使用清单

> **数据依据**：本表全部来自通过 SWD（DAP 调试器）对**运行中板子**的寄存器实读（GPIOx_MODER/OTYPER/OSPEEDR/PUPDR/IDR/AFR、RCC 时钟使能、USART BRR、TIM PSC/ARR、SYSCFG EXTICR），不依赖任何源代码。引脚的"连接对象"一栏是根据配置形态（复用号、电平、时序关系）并结合固件行为推断的标注。
>
> 模式代号：OUT=推挽输出，IN=输入，AF=复用功能，ANALOG=模拟；速度：2=50MHz / 3=100MHz。

## 0. 外设时钟实况

- **开启**：GPIOA、GPIOB、GPIOC、GPIOD、GPIOE；TIM3、TIM4；USART1、USART3；PWR（RTC 在用）；SYSCFG（EXTI 在用）
- **未开启**：GPIOF/G/H（这些端口的引脚全部处于复位态）、TIM2、USART2、UART4、SPI1、SDIO、内部 ADC、DMA
- **采样节拍**：TIM3 运行中，PSC=199、ARR=8399 → **50Hz**（每 20ms 触发一次 AD 转换并输出一行串口数据）
- **TIM4**：PSC=300、ARR=50（≈5.5kHz），当前停止（CEN=0，电机不转时）

## 1. GPIOA

| 引脚 | 实读配置 | 电平 | 连接对象 |
|---|---|---|---|
| PA2 | OUT 50M | 1 | **DAC8562 → SYNC**（片选，空闲高） |
| PA4 | OUT 50M | 0 | **DAC8562 → DIN**（数据） |
| PA5 | OUT 50M | 0 | **DAC8562 → LDAC**（固件也拿它当继电器 Realy0 用，存在复用冲突） |
| PA6 | OUT 50M | 0 | **DAC8562 → CLR**（被固件一直拉低） |
| PA8 | OUT 50M | 0 | **DAC8562 → SCLK**（软件 SPI 时钟） |
| PA9 | AF7 上拉 | 1 | **USART1_TX → 上位机**（实测 115200） |
| PA10 | AF7 上拉 | 1 | **USART1_RX ← 上位机** |
| PA13 | AF0 上拉 | — | **SWDIO**（调试口，系统功能） |
| PA14 | AF0 下拉 | — | **SWCLK**（调试口，系统功能） |
| PA15 | AF0 上拉 | 1 | JTAG JTDI 残留态，固件未接管（源码把它当按键读是无效的） |
| PA0/1/3/7/11/12 | 复位态（浮空输入） | — | 未使用 |

## 2. GPIOB

| 引脚 | 实读配置 | 电平 | 连接对象 |
|---|---|---|---|
| PB0 | IN 上拉 + **EXTI0 下降沿**（EXTICR1 实读确认） | 0 | **AD7606#1 → BUSY**（转换完成中断） |
| PB1 | OUT 上拉 100M | 1 | 输出口（固件初始化置高，具体外接负载需现场确认） |
| PB2 | OUT 上拉 100M | 1 | 同上 |
| PB3 | AF0 | 1 | JTAG JTDO 残留态，未使用 |
| PB4 | OUT 100M | 1 | **步进电机方向 DIR1** |
| PB5 | OUT 100M | 1 | **步进电机方向 DIR2** |
| PB6 | AF2 上拉 100M | 0 | **TIM4_CH1 → 步进电机脉冲 PUL**（电机驱动器） |
| PB7 | AF2 上拉 100M | 1 | TIM4_CH2 引脚已配但通道未使能，实际无输出 |
| PB8 | OUT 100M | 1 | **AD7606#1 → CS**（片选，空闲高） |
| PB9 | IN 上拉 | 1 | **AD7606#1 → DB7/DOUT**（数据入） |
| PB10 | AF7 上拉 | 1 | **USART3_TX**（实测 115200） |
| PB11 | AF7 上拉 | 1 | **USART3_RX ← 串口编码器**（9 字节角度帧） |
| PB12/13 | 复位态 | — | 未使用 |
| PB14/15 | 复位态 | 0 | 未使用 |

## 3. GPIOC（全部属于 AD7606#1 的控制/时序脚）

| 引脚 | 实读配置 | 电平 | 连接对象 |
|---|---|---|---|
| PC0 | OUT 100M | 0 | **AD7606#1 → RST** |
| PC1 | OUT 25M | 1 | **AD7606#1 → RD/SCLK**（软件 SPI 时钟） |
| PC2 | OUT 100M | 1 | **AD7606#1 → CVA**（启动转换 A） |
| PC3 | OUT 100M | 1 | **AD7606#1 → CVB**（启动转换 B） |
| PC4 | OUT 100M | 1 | **AD7606#1 → OS2**（=1，过采样设置 100） |
| PC5 | OUT 100M | 0 | **AD7606#1 → RANGE**（=0 → **±5V 量程**） |
| PC6 | OUT 100M | 0 | **AD7606#1 → OS0** |
| PC7 | OUT 100M | 0 | **AD7606#1 → OS1** |
| PC8~PC13 | 复位态 | — | 未使用（PC8-12 是 SDIO 的 SD 卡引脚，运行时固件未启用 SDIO） |
| PC14/PC15 | 复位态 | 0 | **LSE 32.768kHz 晶振**（RTC 时钟源，振荡器功能不走 GPIO 寄存器） |

## 4. GPIOD（AD7606#2 全部引脚）

| 引脚 | 实读配置 | 电平 | 连接对象 |
|---|---|---|---|
| PD0 | IN 上拉 25M | 1 | **AD7606#2 → DB7/DOUT**（数据入） |
| PD1 | OUT 100M | 0 | **AD7606#2 → OS0** |
| PD7 | IN 上拉 + **EXTI7 下降沿**（EXTICR2 实读确认） | 0 | **AD7606#2 → BUSY**（转换完成中断） |
| PD8 | OUT 100M | 1 | **AD7606#2 → CS** |
| PD9 | OUT 25M | 1 | **AD7606#2 → RD/SCLK** |
| PD10 | OUT 100M | 0 | **AD7606#2 → RST** |
| PD11 | OUT 100M | 1 | **AD7606#2 → CVA** |
| PD12 | OUT 100M | 1 | **AD7606#2 → CVB** |
| PD13 | OUT 100M | 1 | **AD7606#2 → RANGE**（=1 → **±10V 量程**） |
| PD14 | OUT 100M | 1 | **AD7606#2 → OS2**（=1） |
| PD15 | OUT 100M | 0 | **AD7606#2 → OS1** |
| PD2~PD6 | 复位态 | — | 未使用（PD2 是 SDIO_CMD，未启用） |

## 5. GPIOE

| 引脚 | 实读配置 | 电平 | 连接对象 |
|---|---|---|---|
| PE10 | OUT 50M | 0 | 固件按"DAC#2 → CLR"驱动——**实际硬件无第二片 DAC，空挂** |
| PE12 | OUT 50M | 0 | "DAC#2 → DIN"——空挂 |
| PE13 | OUT 50M | 0 | "DAC#2 → SCLK"——空挂 |
| PE14 | OUT 50M | 1 | "DAC#2 → SYNC"——空挂 |
| PE15 | OUT 50M | 0 | "DAC#2 → LDAC"——空挂 |
| PE0~PE9、PE11 | 复位态 | — | 未使用 |

## 6. GPIOF / GPIOG / GPIOH

时钟未开启，所有引脚处于复位态（浮空输入），**全部未使用**。

## 7. 汇总：被固件实际占用的引脚（共 36 个）

```
调试(2)      PA13 PA14  [PA15/PB3 为 JTAG 残留态, 不算占用]
串口(4)      PA9 PA10 (USART1)   PB10 PB11 (USART3)
AD7606#1(12) PB8(CS) PC0(RST) PC5(RANGE) PC2/PC3(CVA/CVB)
             PC6/PC7/PC4(OS0/1/2) PB0(BUSY/EXTI0) PC1(SCK) PB9(DOUT)
AD7606#2(12) PD8(CS) PD10(RST) PD13(RANGE) PD11/PD12(CVA/CVB)
             PD1/PD15/PD14(OS0/1/2) PD7(BUSY/EXTI7) PD9(SCK) PD0(DOUT)
DAC8562(5)   PA8(SCK) PA4(DIN) PA2(SYNC) PA5(LDAC) PA6(CLR)
电机(3)      PB6(PUL/TIM4_CH1) PB4(DIR1) PB5(DIR2)   [PB7 已配未用]
其他输出(2)  PB1 PB2 (初始化置高)
空挂(5)      PE10 PE12 PE13 PE14 PE15 (固件中的"DAC#2", 硬件不存在)
晶振(2)      PC14 PC15 (32.768kHz LSE → RTC)
```

## 8. 备注

- 两片 AD7606 的过采样位实读均为 OS[2:0]=100（16 倍过采样），与旧的 `连线.txt` 文档"OS 接地"不符——**以本表为准**。
- 串口数据流：50Hz、115200 波特、9 列 CSV；其中只有第 9 列是真实 ADC 数据（AD7606#2 的通道，原始码≈6710），第 1~6 列是恒 0 的状态变量，第 7 列是采样计数，第 8 列是目标气压。
- 本清单对应的抓取脚本输出存档于 `live_regs.txt` / `live_regs_full.txt`。
